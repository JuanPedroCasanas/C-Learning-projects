#include <stdio.h> 
 
int main (void) 
{ 
 
return 0; 
} 

/*
(a), (b) and (d) are the same
(c) is not the same, it will require an extra non white space character at the end of the user's input to signify the fact that the whitespace is indeed finished

honestly this one kind of throwed me off, not entirely sure if this is the right answer
*/